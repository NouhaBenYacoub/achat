version: "2"
services:
  mysqldb:
    image: mysql:latest
    restart: unless-stopped
    ports:
      - 3306:3306
    volumes:
      - db:/var/lib/mysql
  achat:
    depends_on:
      - mysqldb
    build: ./
    restart: on-failure
    image: spring-boot
    ports:
      - 8083:8082
    volumes:
      - .m2:/root/.m2
    stdin_open: true
    tty: true
volumes:
  db: